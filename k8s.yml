apiVersion: apps/v1
kind: Deployment
metadata:
  name: kiss-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kiss-user
  template:
    metadata:
      labels:
        app: kiss-user
    spec:
      containers:
      - name: kiss-user
        image: registry.gitlab.com/nestz671/kiss-container-registries/user:0.0.2
        ports:
        - containerPort: 5000
        env:
          - name: MONGODB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: mongodb
                key: mongodb-root-password
          - name: MONGODB_URL
            value: mongodb://root:$(MONGODB_PASSWORD)@mongodb:27017/kiss-user?authSource=admin
        resources:
          requests:
            cpu: 10m
            memory: 50Mi
          limits:
            cpu: 100m
            memory: 100Mi
      imagePullSecrets:
        - name: gitlab-docker-registry
---
apiVersion: v1
kind: Service
metadata:
  name: kiss-user
spec:
  type: ClusterIP
  selector:
    app: kiss-user
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
